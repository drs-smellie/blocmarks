<div class="row">
  <div class="col-md-3">
    <div class="thumbnail">
      <a href="<%= post.url %>" class="thumbnail"><%= post.url %></a>
      <p>
      </p>
        <small>
          submitted <%= time_ago_in_words(post.created_at) %> ago by <%= link_to post.user.user_name %><br/>
        </small>
        <p></p>
        <%= render partial: '/favorites/favorite', locals: { topic: @topic, post: post } %>
        <% if can? :destroy, Post %>
        <p></p>
        <%= link_to "Delete post", [post.topic, post], method: :delete, class: 'btn btn-small btn-danger', confirm: 'Are you sure you want to delete this post?' %>
        <% end %>
  </div>
</div>
